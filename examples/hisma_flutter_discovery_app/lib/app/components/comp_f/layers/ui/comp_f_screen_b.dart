import 'package:flutter/material.dart';

class CompFScreenB extends StatelessWidget {
  const CompFScreenB({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Column(
        children: const [
          Text('CompF - ScreenB'),
        ],
      ),
    );
  }
}
