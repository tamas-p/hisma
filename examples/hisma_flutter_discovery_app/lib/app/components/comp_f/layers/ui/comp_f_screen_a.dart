import 'package:flutter/material.dart';

class CompFScreenA extends StatelessWidget {
  const CompFScreenA({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Column(
        children: const [
          Text('CompF - ScreenA'),
        ],
      ),
    );
  }
}
