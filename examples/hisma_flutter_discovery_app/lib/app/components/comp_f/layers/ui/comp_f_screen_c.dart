import 'package:flutter/material.dart';

class CompFScreenC extends StatelessWidget {
  const CompFScreenC({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Column(
        children: const [
          Text('CompF - ScreenC'),
        ],
      ),
    );
  }
}
