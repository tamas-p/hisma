import 'package:flutter/material.dart';

class CompL2AScreenO extends StatelessWidget {
  const CompL2AScreenO({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Column(
        children: const [
          Text('CompL2A - ScreenO - overlay'),
        ],
      ),
    );
  }
}
