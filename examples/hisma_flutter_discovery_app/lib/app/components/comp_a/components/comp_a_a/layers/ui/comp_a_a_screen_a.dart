import 'package:flutter/material.dart';

class CompL2AScreenA extends StatelessWidget {
  const CompL2AScreenA({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Column(
        children: const [
          Text('CompL2A - ScreenA'),
        ],
      ),
    );
  }
}
