import 'package:flutter/material.dart';

class CompL2AScreenC extends StatelessWidget {
  const CompL2AScreenC({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Column(
        children: const [
          Text('CompL2A - ScreenC'),
        ],
      ),
    );
  }
}
