import 'package:flutter/material.dart';

class CompAScreenC extends StatelessWidget {
  const CompAScreenC({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Column(
        children: const [
          Text('CompA - ScreenC'),
        ],
      ),
    );
  }
}
